name: build html with pandoc

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.1.5
      - uses: docker://pandoc/core:3.1.13
        with:
          args: --citeproc --toc dstt.md -o index.html
